name: Deploy Opsight API

on:
  push:
    branches:
      - main

env:
  IMAGE_NAME: opsight-api

jobs:
  deploy:
    if: github.ref_name == 'main'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Read APP_VERSION
        id: version
        run: |
          APP_VERSION=$(grep '^APP_VERSION=' .env.example | cut -d '=' -f2 | tr -d '\r')
          IMAGE_TAG="${APP_VERSION}-${GITHUB_SHA::7}"
          echo "app_version=$APP_VERSION" >> $GITHUB_OUTPUT
          echo "image_tag=$IMAGE_TAG" >> $GITHUB_OUTPUT
          echo "image=${{ secrets.AZURE_ACR_LOGIN_SERVER }}/${{ env.IMAGE_NAME }}:$IMAGE_TAG" >> $GITHUB_OUTPUT

      - name: Azure login
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: ACR login
        run: az acr login --name ${{ secrets.AZURE_ACR_NAME }}

      - name: Build image
        run: docker build -t ${{ steps.version.outputs.image }} .

      - name: Verify pyodbc in image
        run: docker run --rm ${{ steps.version.outputs.image }} python -c "import pyodbc; print(pyodbc.version)"

      - name: Push image
        run: docker push ${{ steps.version.outputs.image }}

      - name: Deploy to Azure Container App
        uses: azure/container-apps-deploy-action@v1
        with:
          resourceGroup: ${{ secrets.AZURE_RESOURCE_GROUP }}
          containerAppName: ${{ secrets.AZURE_CONTAINER_APP_NAME }}
          imageToDeploy: ${{ steps.version.outputs.image }}
          