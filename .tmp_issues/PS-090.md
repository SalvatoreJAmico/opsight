## Purpose
Harden Phase 4 pipeline orchestration by replacing the current local fallback with authenticated Azure Blob ingestion so the runner works against private storage accounts in non-dev environments.

## Current state
- [x] Pipeline runner executes end-to-end with local fallback
- [x] Ingestion and adapter APIs are aligned with the runner
- [ ] Azure Blob input is authenticated when public access is disabled

## Scope
- Add authenticated read support for Azure Blob CSV sources used by the pipeline runner
- Keep local sample fallback for offline/dev workflows
- Provide clear configuration for storage account, container, and blob path
- Add error handling that reports whether failure is auth, network, or missing blob

## Expected files/modules
- `run_pipeline.py` — use configurable source path and explicit fallback behavior
- `modules/ingestion/ingestion.py` — support authenticated Azure Blob reads
- `configs/storage_config.py` — add source configuration fields if needed
- `README.md` — document setup and run steps for authenticated blob ingestion

## Acceptance criteria
- Pipeline can ingest `opsight_sample_sales.csv` from a private Azure Blob container using credentials
- Pipeline still runs locally via fallback when cloud access is unavailable
- Running `python run_pipeline.py` completes successfully in both cloud-auth and local-fallback modes
- Documentation includes required environment variables and an example configuration

## Short implementation notes
Use Azure SDK authentication (connection string, SAS URL, or managed identity), avoid hardcoded secrets, and surface actionable error messages. Keep runtime behavior deterministic: try configured cloud source first, then fallback only when explicitly enabled by config.
