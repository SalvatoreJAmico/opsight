## Purpose
Create the core intelligence module that will host analytics and machine learning components.

## Responsibilities
- Initialize intelligence module structure
- Integrate intelligence layer with pipeline outputs
- Support future ML components

## Expected files/modules
- `modules/intelligence/` (new directory)
- `modules/intelligence/__init__.py`
- `modules/intelligence/anomaly_detection.py` — anomaly detection engine stub
- `modules/intelligence/scoring.py` — scoring logic stub
- Integration with `modules/persistence/persistence_manager.py`

## Acceptance criteria
- Intelligence module structure exists and is importable
- Module can access persisted pipeline data
- Module can be imported by the pipeline or API layer

## Short implementation notes
Scaffold the `modules/intelligence/` directory with `__init__.py`, `anomaly_detection.py`, and `scoring.py`. Each stub should expose a clear function signature so Phase 7 components can be implemented incrementally. Verify the module is importable from the project root.
